# Copy to .env for local Docker runs.
# GitHub token used by the git-committers plugin (optional).
GithubAccessToken=

# Enable/disable the RSS plugin during local builds.
MKDOCS_ENABLE_RSS_PLUGIN=True
